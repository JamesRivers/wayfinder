# Base cluster resources

Shared Kubernetes resources that everything else depends on.

Expected contents:
- Namespaces
- Common labels / annotations
- Shared secrets and config maps that are not component-specific
